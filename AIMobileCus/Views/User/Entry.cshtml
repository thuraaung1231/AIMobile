@model UserViewModel
@{
    ViewData["Title"] = "User Entry";
}

<body style="background-color:lightcyan">
    @if (TempData["info"] != null)
    {
        <div class="alert alert-success">
            @TempData["info"]
        </div>
    }
    <h3 class="mx-auto">Entry User</h3>
    <form action="/user/entry" method="post">
        <div class="mb-3">
            <label class="form-label">Name</label>
            <input type="text" class="form-control border-bottom" asp-for="Name" placeholder="PLease Fill the Field!">
            <span asp-validation-for="Name" class="text-danger"></span>

        </div>
        <div class="mb-3">
            <label class="form-label">PhoneNo</label>
            <input type="text" class="form-control border-bottom" asp-for="PhoneNo" placeholder="PLease Fill the Field!">
            <span asp-validation-for="PhoneNo" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label class="form-label">Email</label>
            <input type="text" class="form-control border-bottom" asp-for="Email" placeholder="PLease Fill the Field!">
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label class="form-label">Address</label>
            <input type="text" class="form-control border-bottom" asp-for="Address" placeholder="PLease Fill the Field!">
            <span asp-validation-for="Address" class="text-danger"></span>
        </div>

        <div class="mb-3 form-check">
            <input type="checkbox" class="form-check-input">
            <label class="form-check-label">Terms And Conditions</label>
        </div>
        <input type="submit" class="btn btn-primary" value="Entry">
        <input type="reset" class="btn btn-secondary" value="Cancel">
    </form>
</body>